apiVersion: v1
kind: Hpa
metadata:
  name: hpa
spec:
  scaleTargetRef:
    apiVersion: v1
    kind: Pod
    metadata:
      name: hpa-pod
      namespace: hpa-namespace
  selector:
      app: hpa-pod-app
  minReplicas: 1
  maxReplicas: 20
  adjustInterval: 10s
  metrics:
    cpuPercent: 0.5
    memoryPercent: 0.6