apiVersion: v1
kind: Service
metadata:
  name: Service-clusterIP
  namespace: hpa-namespace
  labels:
    app: hpa-pod-app
spec:
  type: ClusterIP
  ports:
    - port: 7081
      targetPort: 7081
      protocol: TCP
      name: file-service
  selector:
    app: hpa-pod-app
  